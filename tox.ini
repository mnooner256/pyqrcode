[tox]
envlist = jyhton, py26, py27, py34


[testenv]
deps=
    pypng
    nose
    coverage

commands =
    coverage erase
    coverage run {envbindir}/nosetests
    coverage report --include=pyqrcode*
